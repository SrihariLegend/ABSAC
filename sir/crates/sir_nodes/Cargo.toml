[package]
name = "sir_nodes"
version.workspace = true
edition.workspace = true

[dependencies]
sir_types = { path = "../sir_types" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
